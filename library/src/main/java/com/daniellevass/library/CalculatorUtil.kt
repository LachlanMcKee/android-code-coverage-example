package com.daniellevass.library

/**
 * Utility class that does simple calculations
 */
object CalculatorUtil{
    fun add(x: Int, y: Int) : Int {
        return x + y
    }

    fun subtract(x: Int, y:Int) : Int {
        return x - y
    }
}